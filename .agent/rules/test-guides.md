---
trigger: always_on
---

* Generate unit test for each file and each method or function even for the ones already created.
*Create a new diectory with the name test under the same directory where the files lives or were genrated.
* Make sure the unit test are named similar to files but with the test_ prefix.
* Document the test.